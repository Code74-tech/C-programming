#include <stdio.h>
int main() {
    int a[10], val, found = 0;
    for(int i=0;i<10;i++) scanf("%d", &a[i]);
    printf("Enter value to search: ");
    scanf("%d", &val);
    for(int i=0;i<10;i++)
        if(a[i]==val) {
            printf("Found at position %d\n
