#include <stdio.h>
int main() {
    int s1, s2, s3;
    printf("Enter marks of 3 subjects: ");
    scanf("%d %d %d", &s1, &s2, &s3);
    int total = s1 + s2 + s3;
    float avg = total / 3.0;
    printf("Total = %d\nAverage = %.2f", total, avg);
    return 0;
}
