#include <stdio.h>
int main() {
    int n, val, sum = 0;
    scanf("%d", &n);
    for(int i = 0; i < n; i++) {
        scanf("%d", &val);
        sum += val;
    }
    printf("Sum = %d, Mean = %.2f", sum, sum / (float)n);
    return 0;
}
