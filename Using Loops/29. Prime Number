#include <stdio.h>
int main() {
    int num, i, flag = 1;
    printf("Enter number: ");
    scanf("%d", &num);
    for(i = 2; i <= num/2; i++)
        if(num % i == 0) { flag = 0; break; }
    if(num < 2) flag = 0;
    if(flag) printf("Prime");
    else printf("Not Prime");
    return 0;
}
