#include <stdio.h>
int main() {
    int i, j, sum = 0, flag;
    for(i = 2; i <= 500; i++) {
        flag = 1;
        for(j = 2; j <= i/2; j++)
            if(i % j == 0) { flag = 0; break; }
        if(flag) sum += i;
    }
    printf("Sum = %d", sum);
    return 0;
}
