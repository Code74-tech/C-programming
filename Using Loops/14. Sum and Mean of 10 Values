#include <stdio.h>
int main() {
    int sum = 0, val;
    for(int i = 0; i < 10; i++) {
        scanf("%d", &val);
        sum += val;
    }
    printf("Sum = %d, Mean = %.2f", sum, sum / 10.0);
    return 0;
}
